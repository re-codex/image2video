# Vertical Video Maker

**Генератор вертикальных видеороликов из изображений и аудио**

Инструмент для сборки вертикальных видео (по умолчанию 1080×1920) из набора картинок и опционального аудиотрека. Подходит для Reels / Shorts / Stories / TikTok и любых вертикальных форматов.

---

## 🎯 Возможности

- 📐 Автоматическое вписывание изображений в вертикальный кадр:
  - сохранение пропорций;
  - центрирование;
  - фон — чёрный или белый.
- 🖼 Поддерживаемые форматы изображений: **.jpg, .jpeg, .png, .webp**
- 🎵 Поддерживаемые форматы аудио: **.mp3, .wav**
- ⏱ Настройки:
  - длительность кадра (`--sec-per`);
  - FPS (`--fps`);
  - размер кадра (`--width`, `--height`);
  - цвет фона (`--bg`).
- 🎧 Обработка аудио:
  - `trim` — обрезать под длительность видео;
  - `loop` — зациклить трек до конца видео.
- 🧾 CLI-интерфейс на `click`:
  - нормальные флаги;
  - `--info` для сводки;
  - `--verbose` для подробных логов.
- 📊 Прогресс-бар в терминале на `tqdm`.

---

## 🧩 Стек и требования

- **Python**: 3.10+ (проект проверен на 3.13)
- **Зависимости** (ставятся из `requirements.txt`):
  - `moviepy` (2.x)
  - `numpy`
  - `Pillow`
  - `imageio`, `imageio-ffmpeg`
  - `tqdm`
  - `click`
- **ffmpeg** должен быть установлен в системе и доступен в `$PATH`  
  (на macOS: `brew install ffmpeg`).

---

## ⚙️ Установка

```bash
git clone <URL-репозитория>
cd vertical_video_maker

# (рекомендуется) создать виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # macOS / Linux
# .venv\Scripts\activate   # Windows

# установить зависимости
pip install -r requirements.txt