# Vertical Video Maker

**Генератор вертикальных видеороликов из изображений и аудио**

Программа для автоматического создания вертикальных видео (1080x1920) из набора изображений и музыки. Идеально подходит для создания контента для Stories, Reels, Shorts, TikTok и других вертикальных форматов.

## 🎯 Возможности

- ✅ **Автоматическая обработка изображений** - масштабирование и центрирование под вертикальный формат
- ✅ **Поддержка форматов** - .jpg, .png для изображений, .mp3, .wav для аудио
- ✅ **Гибкие настройки** - длительность кадров, цвет фона, FPS
- ✅ **Плавные переходы** - дополнительные fade эффекты между кадрами  
- ✅ **Умная обработка аудио** - обрезка или зацикливание под длительность видео
- ✅ **CLI интерфейс** - удобная командная строка с прогресс-баром
- ✅ **Валидация файлов** - проверка форматов и корректности входных данных

## 📋 Требования

- **Python 3.10+**
- **MoviePy** - для создания видеоклипов
- **Pillow (PIL)** - для работы с изображениями
- **tqdm** - для прогресс-бара

## 🚀 Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd vertical_video_maker
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Проверьте установку:**
   ```bash
   python main.py --help
   ```

## 💻 Использование

### Базовое использование

```bash
# Создание видео из папки изображений с музыкой
python cli.py --images ./images --audio music.mp3 --output my_video.mp4

# Создание видео только из изображений
python cli.py --images ./images --output my_video.mp4

# Использование папки с кадрами
python cli.py --images ./frames --audio background.mp3 --output reels.mp4
```

### Расширенные опции

```bash
# Настройка длительности кадров (5 секунд)
python cli.py --images ./images --duration 5 --output video.mp4

# Белый фон вместо черного
python cli.py --images ./images --bg-color white --output video.mp4

# Высокое FPS для плавности
python cli.py --images ./images --fps 60 --output video.mp4

# Зацикливание короткой музыки
python cli.py --images ./images --audio short_song.mp3 --audio-adjust loop --output video.mp4

# Добавление плавных переходов
python cli.py --images ./images --transitions --output smooth_video.mp4

# Подробный вывод для отладки
python cli.py --images ./images --verbose --output video.mp4
```

### Информация о файлах

```bash
# Показать информацию о входных файлах
python cli.py --images ./images --info
```

## ⚙️ Параметры командной строки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--images` | Путь к папке с изображениями или к одному изображению | **Обязательно** |
| `--audio` | Путь к аудиофайлу (опционально) | None |
| `--output` | Путь для сохранения видео | `output/video.mp4` |
| `--duration` | Длительность показа каждого кадра (секунды) | `4.0` |
| `--width` | Ширина видео | `1080` |
| `--height` | Высота видео | `1920` |
| `--fps` | Частота кадров | `24` |
| `--bg-color` | Цвет фона | `black` |
| `--audio-adjust` | Режим подгонки аудио | `trim` |
| `--transitions` | Добавить переходы между кадрами | False |
| `--verbose` | Подробный вывод | False |
| `--info` | Показать информацию о файлах | False |

## 📁 Структура проекта

```
vertical_video_maker/
├── main.py                 # Основной файл
├── cli.py                  # CLI интерфейс
├── requirements.txt        # Зависимости
├── README.md              # Документация
├── modules/               # Основные модули
│   ├── __init__.py
│   ├── image_processor.py # Обработка изображений
│   ├── audio_processor.py # Обработка аудио
│   └── video_maker.py     # Создание видео
├── tests/                 # Тесты (если будут)
├── assets/               # Примеры файлов
└── output/               # Готовые видео
```

## 🔧 Использование в коде

```python
from modules.video_maker import VideoMaker

# Создание экземпляра
video_maker = VideoMaker(width=1080, height=1920, fps=30)

# Создание видео
video_maker.create_video(
    image_paths="./images",
    audio_path="music.mp3",
    output_path="my_video.mp4",
    frame_duration=5.0,
    background_color="white"
)
```

## 🎨 Примеры результатов

### Вертикальные видео для соцсетей
- **Instagram Stories**: 1080x1920, 30 FPS
- **TikTok**: 1080x1920, 30 FPS  
- **YouTube Shorts**: 1080x1920, 30 FPS
- **Stories**: 1080x1920, 24-30 FPS

### Поддерживаемые форматы
- **Изображения**: JPEG (.jpg), PNG (.png)
- **Аудио**: MP3 (.mp3), WAV (.wav)
- **Видео**: MP4 (.mp4) с H.264 кодированием

## 🐛 Устранение проблем

### Ошибка MoviePy
```bash
pip install --upgrade moviepy
```

### Низкое качество видео
- Увеличьте FPS: `--fps 30`
- Используйте более качественные исходные изображения
- Проверьте кодеки в системе

### Проблемы с памятью
- Уменьшите размер исходных изображений
- Используйте меньше кадров за раз
- Закройте другие программы

### Ошибки аудио
- Проверьте формат: только .mp3 и .wav
- Убедитесь что файл не поврежден
- Попробуйте конвертировать в другой формат

## 📝 Лицензия

Этот проект создан для образовательных целей в рамках курса Python.

## 👨‍💻 Автор

Создано в рамках итоговой работы по курсу Python.

---

⭐ Если проект помог вам - поставьте звездочку! ⭐